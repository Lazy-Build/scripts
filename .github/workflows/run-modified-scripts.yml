name: Checks
on: [ pull_request, workflow_dispatch  ]
jobs:
  changed-files:
    runs-on: ubuntu-latest
    # Map a step output to a job output
    outputs:
      all: ${{ steps.changes.outputs.all}}
      ts: ${{ steps.changes.outputs.ts }}
    steps:
      # Make sure we have some code to diff.
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Get changed files
        id: changes
        # Set outputs using the command.
        run: |
          git fetch origin "+refs/heads/${{github.base_ref}}:refs/remote/origin/${{github.head_ref}}"
          echo "::set-output name=all::$(git diff --diff-filter=d --name-only origin/"${{github.base_ref}}")"

  ubuntu-checks:
    name: Run modified scripts to test for errors
    runs-on: ubuntu-latest
    container: 'ubuntu:latest'
    needs: changed-files
    if: ${{needs.changed-files.outputs.all}}
    steps:
      - uses: actions/checkout@v1
      - id: files
        uses: jitterbit/get-changed-files@v1
        with:
          format: 'csv'
      - name: echo changed files
        run: echo ${{needs.changedfiles.outputs.ts}}
  centos-checks:
    name: Run modified scripts to test for errors
    runs-on: ubuntu-latest
    container: 'centos:latest'
    needs: changed-files
    if: ${{needs.changed-files.outputs.all}}
    steps:
      - uses: actions/checkout@v1
      - id: files
        uses: jitterbit/get-changed-files@v1
        with:
          format: 'csv'
      - name: echo changed files
        run: echo ${{needs.changedfiles.outputs.ts}}

  debian-checks:
    name: Run modified scripts to test for errors
    runs-on: ubuntu-latest
    container: 'debian:latest'
    needs: changed-files
    if: ${{needs.changed-files.outputs.all}}
    steps:
      - uses: actions/checkout@v1
      - id: files
        uses: jitterbit/get-changed-files@v1
        with:
          format: 'csv'
      - name: echo changed files
        run: echo ${{needs.changedfiles.outputs.ts}}

  rhel-checks:
    name: Run modified scripts to test for errors
    runs-on: ubuntu-latest
    container: 'richxsl/rhel7:latest'
    needs: changed-files
    if: ${{needs.changed-files.outputs.all}}
    steps:
      - uses: actions/checkout@v1
      - id: files
        uses: jitterbit/get-changed-files@v1
        with:
          format: 'csv'
      - name: echo changed files
        run: echo ${{needs.changedfiles.outputs.ts}}
